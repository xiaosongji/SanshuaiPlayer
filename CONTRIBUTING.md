# 贡献指南

感谢你帮助改进散帅播放器。

## 开始之前

1. 先搜索已有 Issue，确认问题尚未被报告或讨论。
2. Bug 请附系统版本、设备或 Simulator、复现步骤、预期结果和实际结果。
3. 日志、截图和测试数据必须脱敏；不要提交服务器地址、账号、密码、Token、证书、私有音乐、歌词或未经授权的封面。
4. 大型功能或协议扩展请先开 Issue，说明用户价值、平台能力、候选实现、许可证与兼容性。

## 开发要求

- 使用 Swift 6，并保持 iOS 17 最低部署版本。
- 页面只依赖统一领域模型；协议 DTO 和服务差异保留在 Provider 边界内。
- 凭据必须进入 Keychain 或测试专用内存实现，不得写入源码、UserDefaults 或日志。
- 新增依赖前说明维护状态、许可证、二进制体积、隐私和可替换方案，并更新 `THIRD_PARTY_NOTICES.md`。
- 可见 UI 应延续仓库中的 Himhuu Cream / Clay 设计方向，并覆盖深色模式、Reduce Motion 和基础无障碍。
- 新行为应增加测试；涉及播放器、存储或迁移的修改还应验证失败恢复路径。

## 提交 Pull Request

PR 描述应包含：

- 修改目的与用户影响
- 主要实现边界
- 已运行的构建和测试命令
- UI 修改的真实运行截图（如适用）
- 兼容性、隐私或许可证影响（如适用）

请让每个 PR 聚焦一个主题，并避免顺带格式化无关文件。
